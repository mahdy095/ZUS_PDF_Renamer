# Azure Document Intelligence
AZURE_DI_ENDPOINT = "https://your-adi-instance.cognitiveservices.azure.com/"
AZURE_DI_KEY      = "your-adi-key-here"

# Azure OpenAI
AZURE_OAI_ENDPOINT = "https://your-oai-instance.openai.azure.com/"
AZURE_OAI_KEY      = "your-oai-key-here"
AZURE_OAI_DEPLOY   = "o4-mini"
AZURE_OAI_VERSION  = "2024-12-01-preview"

# ── Embedded lookup tables (gzipped + base64-encoded CSV) ────────────────────
# Kept out of the repository because they contain customer data.
# Generate with:
#   import gzip, base64, pandas as pd
#   csv = pd.read_excel("DIC (ve3+type).xlsx").to_csv(index=False).encode()
#   print(base64.b64encode(gzip.compress(csv, 9)).decode())
# Line breaks inside the block are ignored, so wrap it however you like.
DIC_TABLE_B64 = '''
paste-the-base64-blob-here
'''

GESOBAU_TABLE_B64 = '''
paste-the-base64-blob-here
'''
